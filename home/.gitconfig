[user]
	name = SamTolmay
	email = samtolmay@gmail.com

[github]
  user = SamTolmay
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
